# Neon PostgreSQL 数据库配置（用于 Cloudflare Workers）
# 在 Cloudflare Dashboard 的 Worker 设置中添加这些环境变量

# Neon 数据库连接字符串（必需）
# 格式：postgresql://用户名:密码@主机/数据库名?sslmode=require&channel_binding=require
DATABASE_URL=postgresql://neondb_owner:npg_MN0rFxTpGaZ9@ep-broad-darkness-ab8xx6z2-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# 管理员密钥（可选，用于删除域名等管理操作）
ADMIN_KEY=your_admin_key_here

# Resend 邮件发送服务配置（可选）
# 获取 API Key: https://resend.com/api-keys
# 如果不配置，发送邮件功能将不可用
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ==================== 部署说明 ====================
# 
# 1. 在 Neon 控制台执行 database-postgres.sql 创建表结构
# 2. 在 Cloudflare Dashboard 中创建新 Worker
# 3. 将 workers-neon.js 的代码复制到 Worker
# 4. 在 Worker 设置中添加环境变量（DATABASE_URL、ADMIN_KEY、RESEND_API_KEY）
# 5. 配置 Cron Triggers（在 wrangler.toml 中已配置，或手动在 Dashboard 中添加）
# 6. 在域名设置中启用 Email Routing
# 7. 添加 Catch-all 规则，将所有邮件发送到此 Worker
#
# 注意：此方案完全无服务器，无需部署 Node.js 后端

