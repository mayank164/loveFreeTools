{
  "name": "free-email-neon-worker",
  "version": "1.0.0",
  "description": "Neon PostgreSQL Worker dependencies",
  "main": "workers-neon.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

